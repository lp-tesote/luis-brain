---
title: Landing Page Prototypes — State & Handoff
tags: [marketing, website, homepage]
updated: 2026-06-12
status: draft
---

# Landing page prototypes — where things stand

Working folder for the new tesote.com homepage. Strategy/narrative doc: [[landing-page-argument]] (`../landing-page-argument.md`). New session? **Read this file + the argument doc and open the latest `vNN-homepage.html` (currently `v41`) — that's the live candidate.**

> **Pickup for next thread (after 2026-06-17):** CFO-altitude copy sweep on **pagos / cobros / conciliacion** DONE (see "CFO-altitude sweep" section) — Connect convention now applied to all 4 core product pages. **Decision queued for Luis:** keep going page-by-page (next candidates = the Soluciones/Industrias `prod_fit` one-liners, then tesote-ai/integraciones/seguridad) **or** start the **EN forward-port** (homepage parked at `v42-homepage-en.html`, no EN versions of the 15 `page-*.html` yet). External blockers unchanged (Dan stat/security verification, logo consent, Centro de ayuda URL, ERP names, Rails routes) + the deferred **v58 "Tesote IA→AI" homepage hero subcard** fix. Everything still runs through `build-pages-v1.py`.
>
> **Pickup (after 2026-06-12):** Live candidate = **`v57-homepage.html`** + the 15 `page-*.html` files (full-site prototype — see "Sitemap build-out" section). **Page content pass v1 DONE 2026-06-12** (see "Content pass v1" — mock-mismatch fixes on connect/conciliacion, Tesote AI naming, copy fixes on corporativas/seguros); remaining work = the external blockers (stats/security verification, logo consent, Centro de ayuda URL, ERP names) + EN forward-port + Rails routes. One Luis call queued: homepage hero subcard still says "Tesote IA" vs convention "Tesote AI" — fixing it = v58. Current chrome: **FLAT surfaces** (no hero lines/gradients, light CTA band — wave-bars v52, dark band v53, black-gradients v55 all tried & retired) + **Brex-1:1 mega-menu** (v57: container-width sheet, zone headers, blue glyph-tint hover). Color logic stands: blue for data+CTAs / black for chrome+identity (v45), product-UI buttons black (v43). EN parked at `v42-homepage-en.html` — needs forward-port. Iterate site chrome via `build-pages-v1.py` (sources v53 + applies v56/v57 patches), not by editing pages directly.

> *(Historical pickup, 2026-06-07 session):* Live candidate = `v41-homepage.html`. Hero CLOSED (full-viewport, command-center panel). Product rail + all 5 chapter panels DONE & height-standardized. Rail cards wired → chapter anchors (v24). **Negocios chapter renamed → Cobros** (v25). **Story bridge** added between rail and chapters; copy locked v26 = "Después, Tesote transforma tu operación financiera para siempre." Seguridad copy trimmed. **Mock-data rules locked** (see memory `marketing-mock-data-rules`): USD primary, reconcile at BCV 567 but never display rate, Tesote-derivative entities, no real bank names anywhere on the page. **Next up, in order:** (1) **OPEN Q: `#cobros` has no inbound rail card** (5 cards ≠ 5 chapters: Conexión→connect, Tesorería en vivo→connect [2nd connect card], Conciliación→contabilidad, Pagos y cobros→pagos, AI→ai); decide if Cobros needs its own rail card, if "Tesorería en vivo" should repoint, or leave; (2) Dan security-claims Slack checklist [blocks Seguridad shipping]; (3) verify stat-grid numbers vs prod; (4) ~~mobile pass~~ CSS pass DONE v41 (real-device spot-check still ideal); (5) Rails port — **plan drafted: [[rails-port-plan]]** (`rails-port-plan.md`); ships via PR→main→marketing.tesote.com. **Also pending:** Negocios→Cobros rename is landing-page-only so far — product taxonomy memory still lists "Negocios (network bet)"; decide if the public name change is global. Argument doc §2 still lists dead H1 candidates A/B/C + §5/§6 still call it "Negocios" — update to "Cobros" + locked "centro de inteligencia financiera" line when next editing it.

## Version chain (each = self-contained HTML, open directly in browser)

| File | What it is |
|------|-----------|
| `current/index.html` | Pristine offline copy of marketing.tesote.com homepage (dark, eng version). Baseline — never edit. Assets in `current/assets/`, shared by all versions. |
| `v1-wireframe.html` | Earlier from-scratch light wireframe (parallel exploration, pre-Brex direction) |
| `v2-homepage.html` | Dark + Brex-style 5-card product rail (replaced features grid). **Luis liked this design language a lot** — chips/pills/mini-UI cards became the system. |
| `v3-homepage.html` | PARKED. Doc-aligned rail variant (rail below hero, eyebrow+one-liner cards). Luis preferred v2. |
| `v4-homepage.html` | v2 + light theme ("Lunour" palette from Saldos/Movimientos product UI). Done via one token-override `<style>` block in head — Rails port = token change, not redesign. |
| `v5-homepage.html` | v4 + storyline chapters per argument doc (Ve todo / Mueve dinero doors / Libros / AI dark crescendo / Seguridad placeholders) replacing the 3 generic sections |
| `v6-homepage.html` | Doors → separate split rows. Strict Brex alternation: Connect L / Pagos R / Negocios L / Contabilidad R / AI L |
| `v7-homepage.html` | Density pass (`density-pass` style block at end of file): container 1100→1200px, type ceiling down, section padding −25%. Fixed the "zoomed" feel. |
| `v8-homepage.html` | Security section: 6 real claims (AES-256-GCM, TLS, SHA-256 integrity, 2FA, permisos granulares, pentests recurrentes, SOC 2 Type II "en curso" pill, auditoría) |
| `v9-homepage.html` | Real testimonials ×3 (Carlos Domínguez/Ama de Casa, José Daniel López/Cines Unidos, Jorge Guerra/Paisa) — replaced invented "María González" quote |
| `v10-homepage.html` | PARKED. ¿Por qué Tesote? 3-card grid — Luis cut it. |
| `v11-homepage.html` | v9 + "más que un software" woven into testimonios sub + CTA band (consulting-engagement framing). No por-qué section. |
| `v12-homepage.html` | Hero v1: H1 candidate A + command-center sub, kicker Tesorería→Finanzas, primary CTA → "Agenda una demo", dark video replaced with HTML/CSS mini-UI panel (posición consolidada + bank rows + floating Conciliado chip). Meta descriptions synced. |
| `v13-homepage.html` | Hero v2 (Luis 2026-06-07): H1 → "El centro de inteligencia financiera de tu empresa." (new candidate D — fuses vision + AI; supersedes A/B/C) + agents-explicit sub. Panel rebuilt from future-product screenshot (command-center home): action chips Pagar/Cobrar/Transferir/Conciliar, saldo total Bs. + 30-día deltas, 3 cuentas with masked acct numbers, bandeja row ("8 documentos extraídos por IA"). `#hero-pass-v13`. |
| `v14-homepage.html` | Hero panel = gallery-v2 #2 (comando + agente) + Luis's tweaks: action chips squarer (`radius-md`, not pill), saldo USD-primary ($1.245.300,80) with Bs. secondary figure + USD deltas, entity totals replace bank names (Avodon / Aragua / Maralak, dual USD+Bs columns, sums reconcile to the headline figures), bandeja chip → Tesote IA subcard with live agent feed, "Trabajando" badge Stripe-style rectangular (4px radius, sentence case). `#hero-pass-v14`. |
| `v15-homepage.html` | Hero panel final (gallery-v5 #1 "mix equilibrado"): clean panel — floating chips REMOVED, BCV rate REMOVED from display; saldo USD-only **$1.545.220,80** (entity USD + Bs at 567 internally; rate not shown); entities Tesote Tecnología C.A. / Tesote IA / Tesote Tech with dual amounts at EQUAL size+color (Tech solo USD); IA box 4 lines: duplicada detenida $7.490 (amber shield) / 14 facturas cobradas $96.400 / 142 partidas cero diferencias / armando cierre del mes 87% (spinner). `#hero-pass-v15`. |
| `v16-homepage.html` | Cleanup pass (Luis 2026-06-07): hero kicker pill removed, Iniciar sesión button removed (single CTA), hero trust line removed; product section: "Productos" kicker removed, subtitle forced single-line ≥1240px, pcard gradient → flat white + shadow-sm (hover shadow-md), pills → Stripe-style rectangular (4px, sentence case — global, also restyles chapter Conciliado pills), rail card 1 chips → Cuenta Operativa / Cuenta Nómina / Cuenta en USD (no bank names). Stat-grid numbers DISCUSSION PENDING (see open items). |
| `v17-homepage.html` | Stat grid v1 (Luis 2026-06-07): 120+ bancos / 150+ empresas / 100M+ transacciones conciliadas / 86% categorización. Logo-strip heading de-duped → doc §3: "Equipos de finanzas que ya operan con Tesote" (resolves open item 4). |
| `v18-homepage.html` | Stat 4 → **90% · Categorización automática** (shorter one-line label). Numbers still need verification vs real data before ship. |
| `v19-homepage.html` | Above-the-fold experiment: hero + stat-grid padding trimmed so stats load in initial viewport. **Rejected — Luis prefers the clean first paint.** |
| `v20-homepage.html` | v19 trims reverted to v18 spacing — stats below the fold again. |
| `v21-homepage.html` | Hero = full first viewport: `min-block-size: calc(100svh - 64px)` (64px = site navbar `--header-height`), flex-centered; stats start exactly at the fold. **Deliberate decision, don't re-trim to surface stats.** |
| `v22-homepage.html` | Chapter panels standardized (min-height 380px, every panel = cards + footer note): Connect → posición USD $1.545.220,80 + Cuenta-style chips + mini flujo-de-caja chart (entró/salió/neto reconcilable: 84,2−31,5=52,7); Pagos → Lote #88 pipeline (validado→aprobado→ejecutando→ERP) + pago anterior; Negocios → vida del link timeline (enviado→abierto→pagado→conciliado) + 64 cobros/mes; Contabilidad → match+ERP (unchanged) + note; AI → chat+acciones (unchanged) + note. New shared CSS: `.chapter-steps`, `.chapter-flow-*`. |
| `v23-homepage.html` | Seguridad section: "Seguridad" eyebrow removed; H2 "Tu data bancaria…" → "Tu data operativa, tratada como lo que es." (data is more than bank data). |
| `v24-homepage.html` | Rail cards → chapter anchors (open item 3): each `.pcard` is now `<a href="#…">` — Conexión bancaria→#connect, Conciliación automática→#contabilidad, Pagos y cobros→#pagos, Tesorería en vivo→#connect, Tesote AI→#ai. Added `html{scroll-behavior:smooth}`, `.chapter{scroll-margin-top:var(--header-height,64px)}`, `.pcard{text-decoration:none;color:inherit;cursor:pointer}` + `:focus-visible` outline. |
| `v25-homepage.html` | (1) **Negocios → Cobros** rename: chapter `id="cobros"`, eyebrow "Cobros", "Conoce Cobros" link (no rail card pointed to it, so safe). (2) **Story bridge** — new `.story-bridge` interstitial between the product rail and the chapters: narrating lead-in + lifecycle statement (Ver→Pagar→Cobrar→Cuadrar). |
| `v26-homepage.html` | Story-bridge copy locked (Luis): dropped the lead-in + lifecycle line in favor of one dreamy/bold payoff — **"Después, Tesote transforma tu operación financiera para siempre."** Reads as the payoff to the rail header "Conectar tus bancos es solo el comienzo." (setup → proof = cards → payoff). No `em`/color accent — clean single statement. Removed now-unused `.story-bridge__lead` + `em` CSS. |
| `v27-homepage.html` | Story-bridge statement sized to match `.section__title` exactly (`--fs-3xl` mobile / `--fs-4xl` ≥768px) and forced to one line (`white-space: nowrap`; removed the 56ch cap). ⚠️ `nowrap` on a ~62-char line WILL overflow below ~1150px — fold into the mobile pass (open item 7): shrink or allow wrap on narrow viewports. |
| `v28-homepage.html` | Chapter-panel balance pass (Luis): (1) removed the story-bridge `border-top` separator line under the product cards; (2) **leveled up the thin left columns to the Connect pattern** — added a 3-item `chapter__list` to **Pagos** + **Cobros** (Connect & Contabilidad already had one), and lengthened Cobros' one-line body; (3) **AI chat supercharged** (design unchanged — bubbles + 2 action chips + note): now 2 CFO-grade exchanges — runway-if-zero-cobros + AR-aging recovery, and Bs FX-exposure cost. Numbers reconcile with the page ($1.545.220 position, lote #88). L/R alternation preserved (Connect-L · Pagos-R · Cobros-L · Contabilidad-R · AI-L). Panels already uniform at 380px min-height. **AI left stays list-less by design** (crescendo) so its row is intentionally less balanced than the rest. |
| `v29-homepage.html` | Seguridad consolidated 6 boxes → **3** (trust triad: protect data · control access · prove independently): (1) **Cifrado de grado bancario** — AES-256-GCM/TLS/SHA-256 + general data-treatment line (Luis: dropped the explicit "credenciales nunca en texto plano" line — too specific given the move to API-based connections); (2) **Tú controlas quién ve y hace qué** — merges 2FA + permisos granulares + auditoría completa (`fa-user-shield`); (3) **Probado, no solo prometido** — merges pentests + SOC 2 (`fa-certificate`, pill "SOC 2 Type II · en curso"). No new claims added, so Dan's checklist (open item 2) is unchanged. Fills the existing 3-col grid as one row. |
| `v30-homepage.html` | CTA band reframed as consulting-as-product (Luis — "this section can prove critical"): killed the "Más que un software" kicker (folded into subtitle); new lead/title = **"¿Qué porcentaje de tu operación está automatizada?"**; subtitle = "Más que un software, Tesote es el equipo que sube ese número por ti…"; added a 4-bullet `cta-section__list` (2×2 grid → 1-col mobile) productizing the consulting engagement — Diagnóstico · Implementación llave en mano · Especialistas dentro de tu equipo · Soporte dedicado; kept "Agenda una demo" primary + "Lee el blog" secondary, all inside the same centered `bg-surface-1` rectangle. |
| `v31-homepage.html` | (1) Removed "Lee el blog"; CTA's "Agenda una demo" now anchors to `#agenda`. (2) New **`#agenda` booking section** — HubSpot Meetings embed (`meetings-iframe-container` + official embed script). `data-src` is a **PLACEHOLDER** (`meetings.hubspot.com/REEMPLAZAR`); shows a styled placeholder box on `file://` and needs the real Meetings link + http serving to render live. (3) New **FAQ section** — 6 items, native `<details>`/`<summary>` accordion (no JS): bancos · ERP · seguridad · acceso-a-fondos · implementación · precio. **Pricing answer is deliberately number-free** (per pricing discussion below) — scoped in the demo. |
| `v32-homepage.html` | **Launch-candidate trim** (Luis: launch homepage-first, secondary pages don't exist / were vibe-coded). (1) **Header**: killed all 4 mega-menus (Productos/Soluciones/Industrias/Recursos) + the language & country locale selectors; desktop + mobile-drawer nav now = 3 anchors (Producto `#features` · Seguridad `#seguridad` · Preguntas `#faq`) + Acceder (`equipo.tesote.com`) + Agenda una demo. (2) **All CTAs funnel to `#agenda`** — hero, nav (desktop+mobile), and the 5 chapter "Conoce X" links (which pointed to dead deep-pages). (3) **Footer**: 3 lean cols (Navegación anchors / Tesote: login+WhatsApp / Legal: privacidad+terminos) + social kept; dropped the dead product/recursos/sobre cols, the /suscribirse block, and locale switcher. (4) Added `id="faq"`. (5) Meta: removed `og:locale:alternate` (no localized pages); `<title>`/`og:title`/`twitter:title` → locked H1 "El centro de inteligencia financiera de tu empresa — Tesote". Only internal `/paths` left = `/privacidad` + `/terminos` (must be authored before launch) + favicons + brand `/`. |
| `v33-homepage.html` | Real HubSpot Meetings embed wired into `#agenda` — `data-src="https://meetings.hubspot.com/ventas690/reunion-de-ventas-ve?embed=true"` + official `MeetingsEmbed/ex/MeetingsEmbedCode.js`. Kept the styled `booking-frame` + a fallback placeholder (only shows offline). Loads live even from `file://` when online (script is absolute HTTPS). |
| `v34-homepage.html` | Copy tweaks (Luis): nav/mobile/footer link label "Preguntas" → **"FAQ"** (×3; FAQ section H2 stays "Preguntas frecuentes"). Hero entity list: "Tesote IA" → **"Tesote IA, C.A."**, "Tesote Tech" → **"Tesote Tech, Inc."** (first entity "Tesote Tecnología, C.A." unchanged). |
| `v35-homepage.html` | Product rail card 1 (Luis): title "Conexión bancaria" → **"Conexiones Bancarias"** (+ aria-label); body "Saldos y transacciones…" → **"Saldos y movimientos de más de 120 bancos, en tiempo real."** |
| **`v36-homepage.html`** | **CURRENT.** Chapter copy pass (Luis): (1) removed chapter eyebrows for Connect/Pagos/Cobros/Contabilidad — **kept only "Tesote AI"** (testimonios "Clientes" eyebrow also kept; it's not a product); (2) rail title → **"Conexiones bancarias"** (lowercase b); (3) removed `<strong>` on "categorizada y con su contraparte identificada" in the Connect bullet; (4) Connect H2 "Ve todo. Sin entrar a ningún portal." → **"Por primera vez, ve todo tu dinero en un solo lugar."** (CFO-directed: visibility-in-one-place + first-time-ever framing, replacing the operator "portal" frame). |
| **`v37-homepage.html`** | **CURRENT.** CFO-directed chapter H2s (Luis): Pagos "Paga sin entrar al portal…" → **"Aprueba y paga desde un solo lugar — y se concilia solo."**; Cobros "Envía un link de pago…" → **"Cobra con un link de pago: te pagan más rápido y entra conciliado."** (leads with the novel payment-link rail + speed + auto-reconciliation); Contabilidad "Tus libros se llevan solos." → **"Llega al cierre del mes con la conciliación ya lista."** (close-pain). Also removed the 2 remaining chapter-bullet `<strong>` (Pagos "por API bancaria", Cobros "identificado con su factura y contraparte"). **Left bold:** AI chat figures ("4,9 meses"/"7,6 meses" — chat UI emphasis) + CTA consulting bullet leads (not a product chapter). |
| **`v38-homepage.html`** | **CURRENT.** AI chatbox reworked for **traditional VE company CFOs** (not startups — killed runway/burn/"pista" language). Replaced the 2 Q&A exchanges with **one structured weekly digest**: user "Mándame un resumen de la semana pasada" → bulleted digest (Neto +$58.440 / Por qué / Quiénes / Atención) with pumped numbers and coined fictional counterparties (Distribuidora Caruba, Comercial Vethia, Academia Norvel; "tu firma legal" not PTCK; "nómina EE.UU." not Gusto; no bank name on the SMS-fee line). Dollar impact shown as a Bs-balance cost (−$2.180), **rate never displayed** per mock-data rules. New `.ai-digest*` styles (net in success-green, blue section labels, `·` bullets). Pairs with the "Correr cada lunes" chip = recurring Monday digest. |
| `v39-homepage.html` | Weekly digest rebuilt from first principles (CFO-mindset). 5 tight sections: **Neto +$58.440** · **Por qué** (cobros +23%, 88% clientes al día, salida no recurrente $18.600 honorarios) · **Quiénes** (Distribuidora Caruba / Comercial Vethia / Academia Norvel) · **Lo que ya hice por ti** (proof-of-work: concilié 93% en ERP, categoricé 318 mov. / 4 por revisar, detuve duplicada $7.490) · **Atención** (transferencia $9.800 en tránsito; $48.250 a proveedores vencen el jueves — ties to Pagos Lote #88, actionable). Dropped FX line, "23 sin categoría", SMS-fee. The "Lo que ya hice por ti" section is the differentiator vs ChatGPT (only Tesote, ERP-connected, can claim reconciliation). |
| **`v57-homepage.html`** | **CURRENT (ES).** Mega-menu rebuilt as **Brex 1:1** (Luis ref screenshot 2026-06-12; checked Ramp/Rho too, Brex won): flush sheet that drops below the bar and spans the **container width** (logo → CTA button, per Luis — not viewport-wide); sentence-case zone headers + hairline rules; Productos = 2-col item grid + Plataforma column; 44px white icon tiles where the **glyph** tints Tesote blue on row hover (Brex does orange; tile stays white, row tints gray); open trigger = bordered box + caret flips and tints blue; slide-down 180ms; hover bridge; `:focus-within`. `#mega-nav-v57` in the generator. Supersedes the v54 floating-card menu; `nav-menu-gallery-v1.html` option #2 was the closest sibling. |
| `v56-homepage.html` | (ES) — full-site homepage, FLAT chrome. Luis's final call 2026-06-12: hero = **no lines, no gradient** (`.hero::before{display:none}`); CTA band = light surface, **no gradient** (`#cta-flat-v56` kills the compiled blue radial). Clean warm surfaces carry the page. Applied site-wide (all 15 `page-*.html` regenerated). |
| `v55-homepage.html` | (ES) black-gradient experiment (hero + CTA glow in ink at original intensities) — reverted same day to flat (v56). v54 chrome + Luis's 2026-06-12 reversal: (1) **hero wave-bars removed** (v52 motif retired from heroes; lives on in `hero-lines-gallery-v2.html` + v52 if revived); (2) **hero gradient back, in black** — the original blue radial pair returns as `rgba(18,17,15,.22)` + `rgba(18,17,15,.12)` (same intensity, Tesote ink); (3) **CTA band dark treatment (v53) reverted** → light surface with the original radial glow in black `rgba(18,17,15,.18)` (`#cta-glow-v55`; black-on-black was invisible, so glow implies light box = gallery #11 at full intensity). Applied site-wide: all 15 `page-*.html` heroes (`.phero::before`) + CTA bands regenerated. |
| `v54-homepage.html` | (ES) — full-site homepage, wave-bars heroes + dark CTA band (superseded by v55 chrome reversal). v53 + the sitemap build-out: (1) nav → **Brex-style mega-menu** (Productos+Plataforma / Soluciones [tamaño·industria·partners] / Clientes / Recursos), CSS-only hover/focus dropdowns (`#mega-nav-v54`), no Stimulus dependency, drawer = plain-JS toggle; (2) footer → 5 columns with every page; (3) chapter "Conoce X" links now go to the real product pages (was `#agenda`), incl. the **naming fix "Conoce Conciliación automática"** (was Contabilidad). Generated by `build-pages-v1.py` — edit the script, not the file, when iterating the chrome. |
| `v53-homepage.html` | (ES) CTA band → **dark/invert** (Luis picked gallery #17): `.cta-section` now Tesote black `#12110F` (border `#34322E`), blue radial glow killed (`::before{display:none}`), text flipped light (title #FFF, subtitle/bullets #C7C0B5, bold #FFF); blue button + green checks pop on black; gives the page a deliberate light→dark→light rhythm with the AI section. Override-only `#cta-dark-v53` block. |
| `v52-homepage.html` | **(ES) — ✅ HERO LINE MOTIF LOCKED HERE (Luis, 2026-06-11).** Reverted to the **v50 envelope** (Luis didn't love v51's mid-pullback shape) and **boosted the last 8 bars +30%** for an exaggerated final breakout. Strip box raised 260→340px purely for headroom; non-boosted bars sit identically above the fold (anchored `bottom:0`). Blue SQUARE cap on the tallest final crest (t≈0.94). Static SVG. |
| `v51-homepage.html` | (ES) narrative shape (mid-growth → pullback → breakout) — *Luis didn't love it; superseded by v52* |
| `v50-homepage.html` | (ES) Hero strip max 2nd-half growth (t^3.4) — **base shape kept in v52** control-point curve — calm start (~85px) → **big growth into the middle** (~169px) → **pullback after** (~127px) → **breakout to the highest point at the end** (~248px). Light wave texture on top. Blue SQUARE cap on the final breakout crest (t≈0.94). Static SVG. |
| `v50-homepage.html` | (ES) Hero strip max 2nd-half growth (t^3.4, single explosion) strip 230→**260px**, curve **t^3.4** (calm first half, exploding right half — ~90% of the climb in the 2nd half), and **wave amplitude grows with t** so the right side reads energetic. Blue SQUARE cap on last big crest (t≈0.89). Static SVG. |
| `v49-homepage.html` | (ES) Hero strip taller (230px), t^2.2 accelerating growth strip 175→**230px**, **starts higher** off the floor (env floor 0.35), and trend now **accelerates** — `0.35 + 0.65·t^2.2 + 0.11·sin(2.5 cycles)` — so the **big growth lands in the 2nd half of the screen** (hockey-stick), waves riding on top. Blue SQUARE cap on the last big crest (t≈0.89, ~88% across, ~90% tall). Static SVG. |
| `v48-homepage.html` | (ES) Hero baseline strip: bars a little taller (175px) (strip 150→175px, heights 38–160) and the envelope is now **waves on a rising trend** — `0.20 + 0.70·t + 0.12·sin(2.5 cycles)`, so each successive crest/trough sits higher (wobbles but always climbs). Blue **square cap** now rides the highest crest near the right (t≈0.92, ~92% from left) — reads as "current value, top of the uptrend." Static SVG, no JS. |
| `v47-homepage.html` | (ES) Hero line motif = gallery **#17** "baseline strip" (square cap on the wave peak, flat-ish wave): full-width field of 54 thin **wave bars anchored to the bottom edge of the hero** (the fold — "where the page ends"), rising upward. The **peak column is blue with a perfect blue SQUARE cap** (CSS-positioned at the computed bar tip, so it never distorts — vs the gallery's dot). Built off the clean v45 base; static SVG (no JS), `#hero-lines-v47` style: full-bleed strip `bottom:0`, content `z-index:2` above, subtle 3% edge-fade mask, shorter on mobile. **Tunable:** which column is highlighted (now the peak ≈21% from left — could center it), strip height (150px), bar count/opacity. |
| `v46-homepage.html` | (ES) Hero line motif option (gallery **#8**): vertical **wave bars** (signature envelope, 22 bars) behind the command panel, with one **blue bar + blue square cap** as the data accent. Baked as **static SVG** (no JS) into `.hero__media`; `#hero-lines-v46` style adds a left→right fade mask (keeps bars off the headline), z-orders copy(3) > panel(2) > bars(1), and hides them below 992px (hero stacks). Copy stays on clean ground. **Tunable knobs if needed:** bar count/density, opacity (now .9), how far the blue capped bar peeks above the panel, mask reach. |
| `v45-homepage.html` | (ES) Color *logic* locked: **blue = data + CTAs; black = chrome/identity.** (1) Graphs reverted black→**brand blue `#1661E2`** (line+fill unified on 500, dropping the lighter `#3B7CF5`). (2) **WhatsApp FAB** was a blue gradient (blue-700→900) → **Tesote black** + neutral shadow (`#palette-v45` block). (3) **Hero ambient glow** — compiled CSS paints a blue radial wash via `.hero::before`; overridden to a whisper of warm ink (`rgba(18,17,15,.05)`), de-blued. Keeps v43 black product buttons + v44 black section icons. Net: blue only where it means look-here/act/live-data. |
| `v44-homepage.html` | **(ES) — black-forward exploration.** v43 + shifts the dominant decorative blue toward Tesote black (`#12110F`): the 3 area charts (hero spark, Connect cash-flow, Live-treasury card) now draw in black (gradient stops + stroke), and the Seguridad section icons go black. **Intentionally kept blue** = marketing CTAs (`.btn-primary`), chapter "Conoce X" text links, and the small accent info-pills (Programado/Sincronizado/SOC 2) — functional/affordance blue. **Open Q (Luis's call):** do the graphs stay black, or revert to the *true* Tesote blue `#1661E2` (note: v43 graphs use `#3B7CF5`/blue-400, a lighter blue — not the brand 500)? Compare against v43. |
| **`v43-homepage.html`** | (ES) Product-UI buttons → Tesote black. The hero command-panel's primary action chip ("Pagar") was brand blue `--accent`; now `#12110F` (Tesote black) to distinguish product-surface buttons from marketing CTAs (which stay blue, v42). Graphs/accents unchanged from v42. |
| `v42-homepage.html` | (ES) CTA blue fix: `.btn-primary` was rendering `--tesote-blue-800` (#07286B dark navy) because a later non-media rule in the compiled app CSS (`current/assets/`) overrides Bootstrap's default. Added a `.btn-primary` block to the inline `light-theme-overrides` `<style>` (loads after linked CSS → wins cascade) restoring brand blue `--accent` (#1661E2) + `--accent-hover` (#184EB5) on hover/focus/active. Affects all demo CTAs (hero, nav, CTA section, mobile drawer). |
| **`v42-homepage-en.html`** | **CURRENT (EN).** v41-en + the same CTA blue fix. EN now tracks the ES version number. |
| `v41-homepage-en.html` | **EN translation of v41** (pinned to its source version, not a new iteration of the design). Full English homepage: all visible copy, meta/OG (`og:locale` → `en_US`), nav/footer, hero panel, 5 chapters, security, testimonials (real ES quotes rendered in English), CTA, FAQ, logo `alt` text. **Number format switched to US convention** ($1,545,220.80 / Bs 364,070,700.00) — values unchanged. **"IA" → "AI"** throughout (incl. mock entity `Tesote AI, C.A.`). Company proper nouns kept (Grupo Ama de Casa, Cines Unidos, Paisa…). HubSpot Meetings `data-src` left as the real ES booking link. CSS dev-comments left in Spanish (invisible; won't carry to Rails). **Open: H1 EN wording "Your company's financial intelligence center." — Luis to confirm vs alternatives.** Resolves open item 10 at the prototype level; still needs porting to `config/locales/en/pages.rb`. |
| `v41-homepage.html` | Mobile pass (CSS-level; every section's breakpoint accounted for). Fixed the real bug: story-bridge `white-space:nowrap` overflowed below ~1200px → now wraps under 1199px (one-line only on wide desktop, Luis's intent preserved). `#mobile-pass-v41` block: hero drops forced 100svh on phones (≤640px) → content-height; chapter/section/bridge vertical rhythm trimmed on mobile; booking-frame 560px on phones; CTA inner padding trimmed. Hero already stacked (1-col <992px), stat-grid 2×2 <768px, all section grids collapse, logo marquee, hero-panel 100%/max-540px w/ ellipsis — all verified responsive. v40 (dark rail) deleted — Luis didn't love it. |

## Sitemap build-out (2026-06-11) — full-site prototype

**IA locked with Luis** (Brex-pattern, adapted): Productos = Connect / Pagos / Cobros / **Conciliación automática** (naming decision: *Conciliación*, not Contabilidad — "contabilidad" scares the contador). Plataforma = **Tesote AI** / Integraciones / Seguridad. Soluciones por tamaño = Medianas / Corporativas (no startups — VE market). Por industria = Retail y consumo / Alimentos y restaurantes / Distribución y manufactura / **Seguros** (anchored to actual logo evidence; **tech cut** — no proof). Partners = ONE page (no marketplace yet). **Clientes = top-level nav** (strongest asset). Recursos = Blog + Centro de ayuda (webinars deferred until content exists). **No pricing** (sales-led).

> **UPDATE 2026-06-18 — Soluciones restructure (Luis):** menu one-liners reworked + industry list expanded 4→6. **Por tamaño** one-liners now team-size: Medianas "Para equipos de finanzas de 5 a 20", Corporativas "…de 20+". **Por industria** is now: Retail y consumo **masivo** · Alimentos y **Agricultura** (page kept its food-service logos, copy broadened to production/agro) · Distribución y **Manufactura** · **Servicios Financieros** (page-seguros folded in here — reuses the 3 insurer logos as proof, scope broadened to aseguradoras/casas de bolsa/financieras) · **Tecnología** (NEW) · **Petróleo y Gas** (NEW). **This reverses the "tech cut — no proof" call**: Luis said he has logos to add for the new verticals. **page-seguros.html deleted** (→ page-servicios-financieros.html). Partners one-liner → "Integradores, ERPs y Consultores". **⚠️ LOGOS PENDING:** page-tecnologia.html + page-petroleo-gas.html ship with NO logo strip until Luis provides client logos; Servicios Financieros has the 3 insurers as a starting set. **⚠️ OPEN:** Luis referred to the partners zone as "programa de aliados" — confirm whether to rename "Programa de partners"/"Partners" → "aliados" site-wide.

**Generator:** `build-pages-v1.py` — extracts the locked design system from v53 (tokens incl. v42 CTA-blue fix, chapters, pcard/chips, dark CTA v53, wave-bars motif v52, testimonials, FAQ styles) and stamps all pages. **Re-runnable**: edit script → `python3 build-pages-v1.py` regenerates everything (incl. v54). Page heroes use `.phero` (not `.hero`) so the homepage 100svh rule doesn't apply; wave-bars strip at 150px on light heroes; Tesote AI hero is the dark register.

**Files:** `v54-homepage.html` + `page-{connect,pagos,cobros,conciliacion,tesote-ai,integraciones,seguridad,medianas,corporativas,retail,alimentos,distribucion,seguros,partners,clientes}.html`. All CTAs funnel to `v54-homepage.html#agenda` (the one HubSpot embed). Real testimonials only, placed where apt (Cines→Connect/corporativas, Ama de Casa→Pagos, Paisa→Cobros/alimentos/medianas) — never invented quotes.

**Content pass v1 (2026-06-12, later session):** page-by-page review of the 15 pages (the named next step from the sitemap session). Fixed in `build-pages-v1.py` + regenerated (homepage byte-identical — chrome untouched): (1) **page-connect** mock mismatches — "Tu posición consolidada" showed a transaction card and "Cada movimiento llega trabajado" showed the *permissions* panel; now ch1 = new `MOCK_MULTI` (3 homepage hero entities at their USD-equivalents: $857.400,00 + $395.070,50 + $292.750,30 = $1.545.220,80 — reconciles with hero figure, rate never shown) and ch2 = `MOCK_TX` (relabeled "Transferencia recibida · hoy 10:42 am" so the card title no longer duplicates the H2); (2) **page-conciliacion** ch1 "Match automático" showed the sube/baja directions mock (redundant with its own hero) — now new `MOCK_REVISION` (conciliado-solo card + exception card "Lo que queda, lo decides tú"), illustrating the decide-en-un-clic bullet; (3) **page-tesote-ai** agent card "Tesote IA · Trabajando" → **"Tesote AI · Trabajando"** (naming convention; ⚠️ the homepage hero subcard still says "Tesote IA" — locked panel, changing it = v58, Luis's call); (4) **page-corporativas** prod-fit card desc was a raw unverified stat ("100M+ transacciones conciliadas") → benefit copy "El volumen crece sin crecer el equipo." (ties to its pain bullet); (5) **page-seguros** hero sub was pain-only → added payoff "…Tesote lo deja cuadrado, trazable y listo para el regulador." All 5 changed pages screenshot-verified (headless Chrome, 1440px). Reviewed-clean: pagos, cobros, integraciones, seguridad, medianas, retail, alimentos, distribución, partners, clientes (dialect tú-VE ✓, no real bank names ✓, real testimonials only ✓, numbers reconcile ✓).

**Connect deep pass (2026-06-12, Luis-directed):** page-connect rebuilt as the flagship page. (1) **Hero copy** off "bancos en vivo" → the canonical framing: H1 **"Saldos y movimientos de todos tus bancos, en tiempo real."** (aligns with the homepage rail card), centered hero. (2) **Hero image expanded into an app render of the frozen Inicio** (`product/dashboard/prototypes/dashboard-v5.html`, signed off 2026-06-09): browser-framed panel with the **Posición first box** — $1.545.220,80 figure, 30-d area chart with start dot ($1.510.920,80) + end dot, **decomposition band Entró +$84.200 · Salió −$31.500 · Variación neta +$52.700 · Devaluación −$18.400** (reconciles: begin + flujos − dev = end), Por moneda (USD $592.670,30 / Bs. 540.096.133,50 ≈ $952.550,50), Por entidad (the 3 homepage hero entities, same values) — **plus the flujo-de-caja-por-categoría column** (Luis 2026-06-12, after seeing it with a Tesote AI column first; Brex hero ref validated the big-render direction): neto +$52.700 headline, ENTRADAS Cobros de clientes +$76.800 / Otros ingresos +$7.400, SALIDAS Nómina −$14.200 / Proveedores −$9.800 / Impuestos −$4.300 / Servicios bancarios −$3.200 — same 30-d window, reconciles with the decomp band (84.200 − 31.500 = 52.700). Hero layout finalized same day as **copy-left / render-right with flat right-edge bleed** (gallery pick #4 — see "Connect hero exploration"); column foot = Top movimientos (replaced the earlier minis). All marketing mock rules hold (567 internal, never shown; no bank names — dashboard-v5's Mercury/BNC/etc. were NOT carried over; desglose is entity-only). New `#connect-render-v1` CSS block (`CONNECT_CSS`) + `CONNECT_RENDER` in the generator. (3) **Capabilities tour, 4 chapters grounded in the product specs** (`product/connect/{saldos,movimientos}/design.md`): Saldos ("Cada cuenta, cada saldo, al minuto." — agrupación banco/moneda/entidad, estado de conexión + último sync, sync on demand; `MOCK_SALDOS`), Movimientos ("Encuentra cualquier movimiento en segundos." — búsqueda en todo, Mercury filter pills, vistas guardadas, totales en vivo; `MOCK_FILTROS`), "Cada movimiento llega trabajado." (kept, `MOCK_TX`), Equipo ("Hecho para que trabaje todo el equipo." — notas, compartir, permisos+auditoría; `MOCK_EQUIPO` — `MOCK_PERMS` content found its home here). `PANEL_CONNECT` + `MOCK_MULTI` now unused on connect (kept defined). Screenshot-verified desktop; **mobile spot-check pending** (render stacks 1-col <992px, band 2×2 <640px — written, not eyeballed). **CFO-altitude copy correction (2026-06-14, Luis):** the saldos chapter shipped at operator altitude ("la página que reemplaza la ronda de portales… saldo al minuto… estado de conexión/último sync… sincroniza cuando quieras") — that's the treasury-analyst's task, not the buyer's. Reframed to the CFO outcome: H2 "Tu posición de caja, cuando quieras verla.", body = "no se lo pides a nadie y nadie lo arma a mano", bullets = total/desglose · "lo que dicen tus bancos, no un Excel que ya venía viejo" · "disponible cuando entras — tu equipo no para a armarlo". Hero sub "sin entrar a ningún portal" → "sin que nadie arme un reporte". Movimientos chapter H2 "Encuentra cualquier movimiento en segundos" → "Cualquier pregunta, respondida al instante." (lead = your question answered now, not an email to your team). Mock note → "Tu posición real, sin pedírsela a nadie". Search/filter/sync features kept as bullet/mock depth. Now a standing convention (see Conventions).

**TODOs before any of this ships:** (1) Centro de ayuda URL is `#` — confirm real URL; (2) ERP names on Integraciones (Odoo · Profit Plus · SAP) need verification with Dan/eng; (3) industry-page logo use doubles down on the **logo/testimonial consent** blocker; (4) stat grid + security claims still pending verification (same as homepage); (5) eventual Rails routes mapping (page-connect.html → /connect etc.); (6) EN versions of all pages (EN homepage parked at v42-en).

## Connect hero exploration (2026-06-12)

`connect-hero-gallery-v1.html` — 10 hero-section options for page-connect (Luis: try copy-left/render-right per Brex ref screenshot; vary what fills the column below the flujo-de-caja box). Same tokens + render components as the live page; numbers all reconcile with the 30-d band. **1** división 50/50, Posición sola · **2** división, Posición compacta + Flujo apilados · **3** sangrado Brex 1:1 en marco gris cálido (render 2-col completo) · **4** sangrado plano, relleno top movimientos · **5** sangrado plano, relleno saldos por cuenta (estado + último sync) · **6** sangrado plano, relleno atención (por aprobar / en tránsito) · **7** división, flujo de caja protagonista (posición como franja) · **8** división, posición compacta + KPIs semana + top movimientos · **9** recorte inferior estilo Stripe · **10** control = lo actual (centrado + render a lo ancho). Bleed tuned to 126% (148% strangled the cf column); row-nowrap hygiene added to `CONNECT_CSS` (benefits the live page's mobile stack too). **Luis picked #4 (sangrado plano + top movimientos) → BAKED into `build-pages-v1.py` same day.** Bake notes: (a) bleed is NOT a fixed % — `.chero-bleed .app-render` width = `calc(100% + 50vw − 600px + 40px)` (container is 1200px, so the right edge tracks the viewport with a constant ~15px overhang at any width; capped 1080px so ultrawide just floats the box); (b) critical companion rule `.app-render__body > * { min-inline-size: 0 }` — without it the grid columns refuse to shrink below the nowrap rows' min-content and the cf amounts spill past the render border (probe-measured before/after at 1280/1440/1680: amounts now end 25px inside the viewport at every width); (c) split uses the standard `phero__grid` (ratio overridden 0.82/1.18 via `.phero--connect`), stacks <900px where the render returns to 100% width; (d) below the flujo box: Top movimientos · 30 días (Caruba +$12.400 · Nómina quincena −$7.100 · Vethia +$8.900 — inside their category totals); the minis (90% / contrapartes) came out with the swap.

## Conciliación hero exploration (2026-06-18)

`concilia-hero-gallery-v1.html` — 10 hero options for page-conciliacion (Luis: make the hero as impactful as Connect's; goal = communicate **automation** — transactions reconciling immediately, recommendations). Reuses the Connect gallery's head verbatim (same tokens + `app-render` frame + `gh-*` shell) + a new `concilia-render-v1` CSS block (rc-* components: feed rows w/ Conciliado+Auto pills, match pairs, suggestion card, progress ring, exceptions, multi-moneda, ledger, close seal, antes/después). Same copy across all (locked H1 + automation sub); only the render varies. Numbers reconcile (567 internal, **tasa nunca mostrada**; Bs 2.562.840 ↔ $4.520), no bank names, coined counterparties (Caruba/Vethia/Norvel). **Options:** 1 stream/bandeja conciliándose (flat bleed, Connect #4 pattern) · 2 banco↔ERP pares emparejados · 3 recomendación con un clic (suggestion + ghost stack) · 4 la cifra 98% (ring) · 5 excepciones (solo lo que importa) · 6 multi-moneda Bs↔USD · 7 libro mayor (columna Conciliado encendida) · 8 cierre listo (seal) · 9 antes/después · 10 control (panel actual centrado). **Claude's rec: #1 stream** (automation-in-action, matches Connect's flat-bleed winner), with #3 (recomendación) and #5 (excepciones) as strong alternates; #6 multi-moneda is better as a *chapter* than the hero. **Awaiting Luis's pick → bake into build-pages-v1.py → page-conciliacion hero.** Pairs with the pending 4-chapter deepening proposal (match · multi-moneda · ERP al día · cierre).

`concilia-hero-gallery-v2.html` (2026-06-20) — **v1 rejected: all 10 showed the *outcome* (bandeja conciliada, cierre, anillo 98%); Luis wanted the *mechanism*** — "mapeo y reglas configurándose/aplicándose, luego ayuda con matches no obvios." Recon confirmed both are **real product surfaces** (rules DSL → categoría+contraparte+% cobertura in `product/automations/accounting-automation/prototypes/stage-4-rules.html`; explainable match signals + "3 posibles" ranked candidates in `cap-3-recon.html`; rules run **before** AI per `odoo-prd.md`). 7 options, copy held constant (H1 unchanged; sub = "Lo que cuadra, cuadra solo. Lo demás lo decides tú."): **1** motor de reglas (lista DSL→acción+%) · **2** reglas en acción (tag de la regla por fila) · **3** match no obvio (razonamiento explicable: monto exacto·misma contraparte·vence hoy·sin otra ±5d) · **4** candidatos rankeados (94/71/38, top preseleccionado) · **5★** dos capas (reglas+IA en una imagen) · **6** aprende de ti (confirmar→regla) · **7** multi-moneda explicable (Bs↔USD). New `concilia-render-v2` CSS (rv-* classes). NB: dropped v1's 126% bleed in the gallery — it clipped the right-aligned coverage/confidence columns; renders shown contained. **Luis picked #1 (Motor de reglas) → BAKED into `build-pages-v1.py` same day.** Bake notes: new `CONCILIA_CSS` block (`#concilia-rules-v1`: self-contained `app-render` frame + `rc-box`/`rc-head` + `rv-*` rule rows) + `CONCILIA_APP` (built via `_crule(cond,cat,cp,cov)` helper, 5 rules: CARUBA 99 · PAGO VETHIA 98 · NOMINA 100 · COMISION+egreso 97 · POS COMPRA 94; head "47 activas · 71% cobertura"; foot "8 reglas listas para promover a automático"); hero gets new `phero--rules` class (ratio 0.9/1.1, **own `@media ≤900px` stack rule** — unlike `phero--connect` which lacks one) and the render is **contained, not bled** (coverage column must stay visible). Hero H1+sub kept (the v2 sub line is already the page's first chapter heading — would duplicate). Screenshot-verified at 1440px (`.scratch/shots/concilia-page-hero.png`). `PANEL_CONCILIA` now unused (kept defined).

## Pagos reframe — AP product + bank-grade security (2026-06-18)

Luis flagged Pagos as a **regulatory grey area**: telling buyers "pay without logging into the bank" reads as a yellow flag (banks enforce MFA/multi-step approval for a reason; "are you as serious as the bank? who guarantees it?"). Reframed the whole page on two axes: **(1) Accounts-Payable product** (supplier invoice → created in Tesote + your ERP → you approve & schedule → pay, individual or en masse) and **(2) bank-grade security via the bank's own API** — we don't bypass bank controls, we ride the bank's official API which carries its grade of security + its exact approval/release process (the same pattern enterprises already automate ERP→SFTP→SAP→bank, done integrated).

Page now has **4 chapters** (was 3): (1) "El pago empieza en la factura, no en un formulario." [new AP-intake, `MOCK_AP_INTAKE`] · (2) "Tú apruebas y programas. Un pago o quinientos." [`MOCK_VALIDACION`] · (3) "Con el grado de seguridad de tu banco, no a pesar de él." [reworked `MOCK_API`] · (4) "El cierre no acumula trabajo." [`MOCK_ERP_INSTANT`]. Killed all "sin portal / sin entrar al banco / Portal del banco: No hace falta / Directo por API bancaria / sin repartir las llaves" phrasing. Hero sub + meta retitled to the AP framing. **Same yellow-flag language also fixed on the homepage Pagos chapter** (edited in `v53-homepage.html` source → propagates to v57: body, first bullet, panel note). Grep-verified zero risky phrasing remains in the current set; screenshot-verified.

## Logo set expansion (2026-06-18)

Luis sent 19 new client logos to add to industry proof rows + the Clientes master wall. **Asset-format gotcha discovered:** the site's logo treatment is `grayscale + brightness(0.25)` (in `PAGE_CSS .logo-wall img`), which only works on **transparent** artwork — the 23 original `.webp` logos are white-artwork-on-transparent (the filter darkens them to gray on the light page). Of the 19 new files, only **6 were transparent** (Yummy, Corporación Inveca, Promaker, Balú, Canguro, Farmabien) — the other **13 have solid backgrounds and render as dark boxes**. (A color-on-white-tile alternative was previewed but rejected: it makes the new color logos pop but turns all 23 white-artwork originals invisible — would require re-sourcing everything in color.)

**Decision (Luis): keep the monochrome treatment.** The 6 transparent logos are wired in (Yummy→Tecnología, Inveca→Alimentos, Promaker→Distribución, Balú/Canguro/Farmabien→Retail; all 6 also in the Clientes master wall). The 13 solid-bg logos were **pulled** from all surfaces (files still on disk in `current/assets/clients/` but unreferenced) pending **transparent-PNG re-exports**.

**⚠️ 13 logos awaiting transparent PNG re-export** (then re-add to mapped page + master wall). JPEGs must become PNG:
| Logo | Mapped industry page | current file → target |
|---|---|---|
| Cashea (fintech) | Servicios Financieros | cashea.png → re-export transparent |
| Tu Gruero (tech) | Tecnología | tu-gruero.png |
| Socado, Inquiport, Protinal Proagro, Alvarigua | Alimentos y Agricultura | socado.png / inquiport.png / protinal-proagro.jpeg→.png / alvarigua.jpeg→.png |
| Covencaucho | Distribución y Manufactura | covencaucho.png |
| Tu Aliado, Latinbien, Mangocenter, Farmacias San Ignacio | Retail y consumo masivo | tu-aliado.jpeg→.png / latinbien.png / mangocenter.png / farmacias-san-ignacio.jpeg→.png |
| Clínica Fénix, Prevaler (hospital/medicine) | Clientes wall only (no health vertical) | clinica-fenix.png / prevaler.jpeg→.png |

**Also still open:** **Petróleo y Gas has zero logos** (none of the 19 fit) — needs its own proof. And Luis mentioned wanting to *remove* a few existing logos — list not yet given.

## CFO-altitude sweep — pagos / cobros / conciliacion (2026-06-17)

Extended the Connect convention (Conventions §3) to the other three product pages. Hero **H1s kept** (they're the locked v37 homepage chapter H2s); rewrote each **hero sub + chapter H2s** to lead with the buyer's outcome and demoted operator/feature framing to the bullets/mocks (untouched). All in `build-pages-v1.py`, regenerated; homepage byte-identical; screenshot-verified at 1440px.

- **Pagos** — hero sub off "Pagos directo por API bancaria…" → "apruebas una vez y no queda nada por revisar después." Ch H2s: "Cada lote se valida antes de salir" → **"Ningún pago equivocado sale de la empresa."**; "Ejecución directa por API bancaria" → **"Pagas a todos tus bancos sin repartir las llaves."** (control angle, not the banned "sin entrar al portal"); "Conciliado y en tu ERP, al instante" → **"El cierre no acumula trabajo."**
- **Cobros** — hero sub leads with "cobras más rápido y no queda nada que conciliar." Ch H2s: "El link lleva la factura adentro" → **"Te pagan más rápido, sin perseguir a nadie."**; "Tu cliente paga como prefiera" → **"Mientras más fácil le sea pagar, más rápido cobras."**; "Y no hay nada que conciliar" kept (already buyer-outcome).
- **Conciliación** — hero sub off "Tesote habla con tu ERP en ambas direcciones" → "el cierre… pasa a estar siempre listo." Ch H2: "Match automático, banco contra ERP" → **"Lo que cuadra, cuadra solo. Lo demás lo decides tú."**; "Tu ERP, siempre al día" kept (already buyer-outcome).

**Remaining page copy still at operator/feature altitude:** the Soluciones/Industrias pages (medianas/corporativas/retail/alimentos/distribucion/seguros) use `pains_sec` + `prod_fit` — pains are already buyer-framed, but the prod_fit one-liners and some pain bullets could get a light pass if we keep going page-by-page. tesote-ai / integraciones / seguridad / partners / clientes not yet swept.

## Mega-menu style exploration (2026-06-12)

`nav-menu-gallery-v1.html` — 6 working hover-menu treatments (same nav, real items, only design/motion changes; all CSS-only so they port 1:1 into the generator): **1** refined card (layered shadow, rise-in, column dividers, icons fill black on hover) · **2** Brex full-width panel (slides down, most "platform company") · **3** scale-from-origin (Apple-calm, blue title tint) · **4** stagger cascade (items animate in sequence) · **5** dark/ink panel (black identity, blue icon fill) · **6** connected pill+notch (trigger → black pill, panel pointer). All have caret rotation, hover-bridge (no flicker), `:focus-within`. **Awaiting Luis's pick (combos OK) → bake into `build-pages-v1.py` → v57 + regenerate pages.**

## CTA-band attention exploration (2026-06-11)

`cta-band-gallery-v1.html` — 20 ways to draw the eye to the "¿Qué porcentaje…" CTA band, replacing the current **blue radial glow** (`.cta-section::before` in `current/assets/` = `radial-gradient(circle at top, rgba(22,97,226,.18))` — off-brand tech-blue, same wash we removed from the hero). The band is the same warm color as the page, so it *needs* a differentiator. Groups: tonal (2–3) · elevation/shadow (4–5) · frame/border (6,16,20) · accent rule/bar (7–10) · on-brand glow/gradient (11–12) · line texture (13–15, incl. wave-bars echo) · dark/invert (17–19). **Claude's rec: #17 dark band** (invert the page — max contrast, blue button pops, consistent with the AI section's dark register), or **#4/#5 white+shadow** if staying light, or **#15 wave-bars echo** for hero consistency. Awaiting Luis's pick → bake into `vNN`.

## Hero line-motif exploration (2026-06-11)

`hero-lines-gallery-v2.html` — **20 iterations in the actual Tesote house style** (Luis shared 2 reference images: (a) vertical bars on a wave envelope + one blue line w/ blue square cap; (b) horizontal lines with a left→right opacity fade + blue 100%/0% axis ticks). The brand motif = **confident black line-fields shaped by data, blue as a single precise accent** — reconciles with the palette logic (blue = the data point that matters). Rendered procedurally (JS) for fast exploration; **the winner gets baked as static SVG/CSS into a `vNN` — no JS in the production page.** Families: vbars wave/growth/double/bell/valley + caps (square/dot/diamond) + density + placement (full/right/strip) + selected-range + callout; hlines fade (R/L/down/center) + blue-line variant + axis ticks; #18 hybrid; #19–20 animated (grow-in, reduced-motion safe). **#1 = ref-1, #9 = ref-2.** Awaiting Luis's pick → tune → push to `vNN`. (v1 below was the first, too-subtle pass — superseded.)

`hero-lines-gallery-v1.html` — *(superseded by v2)* 10 subtle **black-line** hero treatments (Luis: "we love lines… black lines… something super subtle"). Same Spanish hero + simplified command panel + tokens across all 10; only the background line layer changes. All lines = brand ink `#12110F` at low alpha, most soft-radial-masked so they fade toward edges and don't fight the panel. Options: **1** ledger (horizontal) · **2** columns (vertical) · **3** graph grid · **4** diagonal hatch · **5** topographic contours (SVG, most "designed") · **6** dot grid · **7** editorial (single margin rule + baseline hairline, most minimal) · **8** animated H1 underline-draw · **9** animated ambient flow-sparkline (draws + drifts, ties to product) · **10** crosshair/plus technical grid. 8 & 9 respect `prefers-reduced-motion`. **Awaiting Luis's pick → tune opacity/spacing/mask on the winner → push into a `vNN`.**

## Hero image exploration (2026-06-07)

`hero-gallery-v1.html` — 10 hero-panel concepts side by side (same Lunour primitives, shared `.hp-*` classes): 1 command center (v13) / 2 gran saldo / 3 bandeja IA / 4 agente trabajando / 5 conciliación en vivo / 6 multi-entidad / 7 pago en vuelo / 8 link de cobro / 9 collage editorial / 10 app en miniatura. **Luis picked concept 1 (centro de comando) to iterate.**

`hero-gallery-v2.html` — 10 iterations of the centro de comando (all multi-surface per Luis's direction): 1 base v13 / 2 comando+agente feed / 3 dos columnas / 4 con tabs de navegación / 5 panel+satélites / 6 timeline del día / 7 cuatro KPIs / 8 entra-y-sale / 9 aprobación pendiente / 10 miniatura completa (app frame). **Luis picked #2 → became v14** (with tweaks: squarer chips, USD primary, entity rows, Stripe badge).

`hero-gallery-v3.html` — 8 refinements of comando+agente under Luis's number rules (2026-06-07): **USD always primary; Bs per entity matters; everything reconciles at BCV 567,00** ($1.245.300,80 = Bs. 706.085.553,60); entities = Tesote derivatives (Comercial/Distribución/Servicios). Variants: 1 base corregida / 2 tasa BCV in bar / 3 toggle USD-Bs / 4 Bs inline abreviado / 5 Bs como franja / 6 dos columnas / 7 agente primero / 8 dos monedas lado a lado. **Luis picked #1 with new rules → gallery v4.**

`hero-gallery-v4.html` — 10 final versions of v3 #1 under Luis's 2026-06-07 rules: top figure USD-only **$1.545.220,80** = entity USD ($592.670,30) + entity Bs converted at 567 ($952.550,50); entities = Tesote Tecnología C.A. (Bs+USD) / Tesote IA (Bs+USD) / Tesote Tech (USD only), dual amounts same size+color; IA feed expanded to "wow" jobs; 2 floating chips (cobro conciliado + lote para tu firma) never covering the IA card. Variants differ by IA repertoire + chip placement: 1 base / 2 guardián / 3 cobradora / 4 contadora / 5 vidente (proyección) / 6 multitarea (recommended) / 7 chat "pídeselo tú" / 8 bitácora del día / 9 resultados-ROI / 10 hecho-vs-para-tu-firma. **Luis's follow-up: kill floating chips entirely, drop BCV rate from label, IA box is the open question.**

`hero-gallery-v5.html` — 8 compositions of ONLY the Tesote IA box (panel otherwise locked: clean, no floats, no BCV; label "Saldo total en USD · Todas las entidades"), built from Luis's 7 liked feed lines: 1 mix equilibrado 4L / 2 plata primero 3L / 3 ciclo completo 5L / 4 cobranzas en vivo 3L / 5 cierre con barra de progreso / 6 guardián+cobradora 4L / 7 hecho-vs-en-curso split / 8 rotación implícita (3L + "+23 tareas hoy"). **Luis picked #1 → became v15. Hero CLOSED.**

## Chapter panel exploration (2026-06-07)

`chapter-gallery-v1.html` — chapter panels standardized to one frame (400px min-height, 2 ui-cards + footer note) + 3 options per product. Connect: A posición+movimiento / B muro de bancos / C transacción inteligente. Pagos: A orden+lote / B pipeline / C aprobar-desde-donde-estés. Negocios: A link+te-pagaron / B vida del link / C suscripciones. Contabilidad: A match+ERP / B cierre en vivo / C documento→asiento. AI (dark): A chat+acciones / B agente nocturno / C reporte que se hace solo. **Luis picked (2026-06-07): Connect = custom graft (posición consolidada USD + mini flujo-de-caja chart) / Pagos B pipeline / Negocios B vida del link / Contabilidad A / AI A → v22.**

## Key decisions made (2026-06-06 session)

- **Build on marketing.tesote.com** (what's in the `tesote/tesote.com` Rails repo), not tesote.cloud restart
- Light theme = Lunour palette (same warm-gray ramp + #1661E2 blue as product UI)
- Product rail keeps v2 style: title + body + mini-UI visual (NOT v3's eyebrow/one-liner style)
- "Conciliado" green pill = the visual refrain, repeated in rail + Pagos + Negocios + Contabilidad
- AI chapter = page's only dark section (doc: "shifts into the future")
- Testimonial trio with logos > doc's single-quote spec
- ¿Por qué Tesote? grid killed; "more than software" lives in testimonios sub + CTA band instead
- CTA: "¿Cuánto tiempo pierde tu equipo cerrando el día?" kicker "Más que un software"
- SHA-256 phrased as *integridad* (it's a hash, not cifrado) — keep this correction

## Open items (prod-ready gap)

1. ~~**Hero**~~ — DONE in v13. H1 locked by Luis: "El centro de inteligencia financiera de tu empresa." (supersedes argument-doc candidates A/B/C — update doc §2 when next editing it). Panel = future-product command-center home, modeled on Luis's 2026-06-07 screenshot. Hero video asset no longer referenced.
2. **Dan sign-off on security claims** — TLS version, AES mode, pentest cadence, SOC 2 status. Draft Slack checklist before port.
3. ~~**Wire rail cards → chapter anchors**~~ — DONE in v24 (cards are now `<a href="#…">` with smooth scroll). **Residual:** `#negocios` has no inbound rail card (only 5 cards, and "Tesorería en vivo" is a 2nd Connect-themed card). Decide: add a 6th card, repoint "Pagos y cobros" → #negocios, or leave as is.
4. ~~**Logo strip heading**~~ — DONE in v17 (doc §3 line; 150+ stat now lives only in the stat grid)
5. **Stat grid claims** (120+ bancos / 150+ empresas / 100M+ transacciones conciliadas / 90% categorización automática) — locked in v18, but verify every number against real data before ship
6. **Light-theme rough edges**: client logos use `grayscale+brightness(.25)` filter (eyeball each); wordmark is CSS-inverted (swap to dark asset in repo) — hero video removed in v12, no longer an issue
7. **Mobile pass** — only basic breakpoints written; never visually checked
8. **Stimulus JS dead on file://** — mega menus/carousel untested in light theme. `python3 -m http.server` to test.
9. **Port to Rails** (`tesote/tesote.com` repo): prototype styles live in 3 `<style>` blocks (head token-override / product-rail / chapters+testimonios) + `density-pass` at end of file — designed to map to SCSS tokens + ViewComponents
10. ~~**EN version**~~ — DONE at prototype level in `v41-homepage-en.html` (full English homepage). **Residual:** confirm H1 EN wording; port to `config/locales/en/pages.rb` on the Rails side; have a native-English read for VE-market-aware phrasing if desired.
11. **HubSpot Meetings link** (v31) — booking section `#agenda` has a placeholder `data-src` (`meetings.hubspot.com/REEMPLAZAR`). Get the real Meetings link from Luis, drop it in, and serve over http (embed script is dead on `file://`).
### Launch checklist (after v32 trim — homepage-first launch)

**Blockers (must clear before public):**
- [ ] **Stat-grid numbers** verified vs prod (item 5) — don't ship unverified 120+/150+/100M+/90%
- [ ] **Security claims** signed off by Dan (item 2) — AES mode / TLS version / pentest cadence / SOC 2 status; appears in both Seguridad section AND FAQ
- [ ] **Client logos + named testimonials** (Cines Unidos etc.) — written consent to use names/logos/quotes publicly
- [ ] **Privacy Policy + Terms** pages authored & live (footer links `/privacidad` `/terminos`) — required for HubSpot lead capture
- [x] **Real HubSpot Meetings link** wired into `#agenda` (v33)
- [ ] **`og:image`** still points to the OLD dark dashboard (`marketing.tesote.com/assets/...treasury-dashboard.webp`) — swap to a new light-hero share image (couldn't fix in proto — needs a real asset)
- [ ] **Mobile pass** (item 7) — never visually checked; story-bridge `nowrap` overflows on phones

**Done in v32:** lean header/footer, all CTAs → `#agenda`, dead secondary-page links removed, titles synced to locked H1, locale alternates removed.

12. **Pricing** (open strategic call, v31) — genuine range $500–$7k/mo, 14× spread driven by # bancos/entidades/volumen/scope. Decision so far: **don't publish a number** (sales-led, per argument-doc deliberate omission); FAQ answers "depends, scoped in the demo". Revisit if we want a qualifying floor ("desde $X") — see pricing discussion in thread.

## Conventions

- Every change = new `vN-homepage.html` (Luis wants the visible diff chain; never edit a prior version)
- Copy: Spanish-first VE, `tú`, "finanzas" never "tesorería" (client quotes verbatim exempt)
- **Copy altitude = the CFO/economic buyer, not the operator** (Luis, 2026-06-14). Section H2+body lead with the *buyer's outcome* — self-serve cash position on demand, no team toil, no "requesting a report and getting a stale Excel" — NOT the analyst's task. Kill operator-as-value-prop lines: "reemplaza la ronda de portales", "sin entrar a ningún portal", "sincroniza cuando quieras", "al minuto / último sync", "encuentra cualquier movimiento". Operator features still live in the bullets/mock as depth for the evaluator in the room, never as the headline. Real-time is a supporting truth, not the hook. See memory `feedback_marketing_copy_cfo_altitude`. (Connect page rebuilt to this on 2026-06-14; **pagos/cobros/conciliacion swept 2026-06-17** — see "CFO-altitude sweep" below.)
- No emojis in page UI — fontawesome icons / inline SVG only
- Mini-UI visuals built in HTML/CSS (chips, pills, charts), never screenshots/illustrations
