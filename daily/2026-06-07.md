---
title: 2026-06-07 — Landing page marathon: site snapshot → v23 homepage prototype
tags: [daily, session-log, marketing, website, homepage]
updated: 2026-06-07
---

# 2026-06-07

## Session — landing page marathon (ran 2026-06-06 midday → past midnight)

One long arc: pulled the live site, then iterated the homepage through 10 versions to a near-complete prototype. **Live candidate: [[../marketing/landing-page/README]] → `v11-homepage.html`** — the README is the handoff brief (version chain, decisions, prod-gap list); next session starts there, not here.

**Setup**
- Full HTML pull of marketing.tesote.com → `marketing/site-snapshots/marketing-tesote-com-2026-06-06/` (29 ES + 28 EN pages + compiled CSS/JS). Regional variants (ve/mx/do) are copy-identical to base — verified by diff, skipped. Copy changes only ever need doing once per language.
- Decision: **iterate on top of marketing.tesote.com** (it's what's in the `tesote/tesote.com` repo), not restart from tesote.cloud. Supersedes the 2026-06-05 "never the reference" stance — it's the base now, though its *copy* still gets replaced per [[../marketing/landing-page-argument]].
- `marketing/landing-page/current/` = pristine offline-viewable baseline (all 56 assets local, relative paths).

**The version chain** (each = new file, visible diff; v3 + v10 parked)
- v2 — features grid → **Brex-style 5-card product rail**: title + body + mini-UI visual (CSS chips/pills/charts, no images). This design language became the system for the whole page. I liked this one a lot.
- v4 — **light theme via one token-override block**: the site CSS already uses the same warm-gray ramp + #1661E2 as the Lunour product palette (Saldos/Movimientos), just pinned dark. ~20 semantic tokens remapped → whole page flips. Rails port = token change, not redesign. Key insight of the day.
- v5/v6 — **storyline chapters** per the argument doc replacing the generic mid-page sections: Ve todo / Pagos / Negocios / Libros / AI. Doors (§5) became two full split rows → strict Brex L/R/L/R/L alternation. AI = the page's only dark section (crescendo register, per doc).
- v7 — **density pass**: page felt "zoomed". Root cause: 1100px container with 4rem headlines. Canvas → 1200px, type ceiling down, section padding −25%. All knobs in one `density-pass` block at file end.
- v8 — **Seguridad real**: AES-256-GCM reposo / TLS tránsito / SHA-256 *integridad* (not "cifrado" — it's a hash; keep this correction), 2FA, permisos granulares, pentests recurrentes, SOC 2 Type II with honest "Certificación en curso" pill, auditoría. ⚠️ Dan sign-off pending before ship.
- v9 — **real testimonials ×3** (Carlos Domínguez/Ama de Casa, José Daniel López/Cines Unidos, Jorge Guerra/Paisa) — kills the invented "María González" quote that was live on the eng page. Beats the doc's one-quote spec.
- v11 — **"más que un software"** woven in after killing the ¿Por qué Tesote? card grid (2 of its 6 cards were redundant with new sections; grids are where that message dies). Lives instead in: testimonios sub ("Ninguno implementó solo…") + CTA band (kicker "Más que un software", consulting-engagement subtitle). Page closes: clientes reales → empresa seria → demo.

**Refrain made visual:** the green "Conciliado" pill appears in the rail + Pagos + Negocios + Contabilidad — "se concilia solo" compounds as you scroll without being stated.

**Caught along the way (liabilities on the live page):** broken hero subline ("…que las llevan al día" — dangling antecedent, also the sitewide meta description) · fake testimonial · Zara/Burger King logos in the client strip (franchise operators? needs a conscious yes) · unverified stat claims (120+/$1B+/150+/99.9%).

**Pending / next (full list in the README):**
- Hero copy — doc candidate A: "Todos tus bancos. Un solo lugar. En tiempo real."
- Dan: security-claims checklist (Slack draft offered, not yet sent) + stat-grid verification
- Rail cards → chapter anchor wiring; logo-strip heading vs testimonios H2 dedupe
- Mobile pass (breakpoints written, never eyeballed); Stimulus JS untested in light theme (dead on file://)
- Port to Rails repo as SCSS tokens + ViewComponents; EN copy
- Doc hygiene done: argument doc has a "Prototype state" delta section; memory updated to point at the README.

---

## Late session — hero rebuilt + chapter panels standardized (v11 → v23)

Picked the marathon back up and drove the hero + product sections to near-final. **Live candidate now `v23-homepage.html`** (README pickup block updated). Same method as the morning: reference → galleries → pick → port to a new vNN. Five galleries this round (hero ×4, chapters ×1).

**Hero — closed.**
- H1 went vision-first. Killed doc candidates A/B/C in favor of Luis's line: **"El centro de inteligencia financiera de tu empresa."** — fuses command-center + AI into one phrase nobody in VE can say credibly. Sub: agents-explicit ("agentes de IA que ejecutan y concilian por ti"). *Argument doc §2 still lists the dead A/B/C — needs updating next edit.*
- Visual: dark eng video → HTML/CSS **command-center panel** (modeled on the real product home screenshot). Arc: bank-rows → entity totals → "comando + agente" with a live IA feed. Final = gallery-v5 #1 "mix equilibrado": duplicada detenida $7.490 / 14 facturas cobradas $96.400 / 142 partidas cero diferencias / cierre del mes 87%.
- Stripped: kicker pill, Iniciar-sesión button (single CTA now), trust line. Hero = **full first viewport** (`min-block-size: calc(100svh − 64px)`, flex-centered) — Luis decided the clean first paint (no stats peeking) beats above-the-fold stats. Don't re-trim.

**Mock-data rules locked** (saved to memory [[marketing-mock-data-rules]]): USD always primary; Bs at entity level; everything reconciles at **BCV 567** but the rate is **never displayed**; entities = Tesote derivatives (Tecnología/IA/Tech), **no real bank names anywhere on the page**; equal visual weight for USD+Bs; no floating chips (cleaner). Why: credibility with a finance audience (numbers that cuadran) + clean panels at hero scale.

**Stat grid:** 120+ bancos / 150+ empresas / 100M+ transacciones conciliadas / 90% categorización automática. Reworded "procesadas"→"conciliadas" (truer to the product, feeds the villain narrative); de-duped 150+ from the logo strip (heading → doc §3 "Equipos de finanzas que ya operan con Tesote"). **All numbers still need prod verification before ship.**

**Product rail:** dropped the dark gradient → flat white + shadow (matches Lunour `ui-card` elsewhere); pills → Stripe-style rectangular (global, also restyles chapter pills); chips → Cuenta Operativa/Nómina/USD, no bank names.

**5 chapter panels — height-standardized** (min-height 380px, all = cards + footer note, so the L/R alternation reads as a system). Picks: Connect = posición USD + **mini flujo-de-caja chart** (entró/salió/neto reconcilable) / Pagos = **pipeline** (validado→aprobado→ejecutando→ERP) / Negocios = **vida del link** timeline (fixes the skimpy panel) / Contabilidad = match+ERP / AI = chat+acciones.

**Seguridad:** removed "Seguridad" eyebrow; H2 "Tu data bancaria…" → **"Tu data operativa…"** (it's more than bank data now).

**Consequence to weigh:** bank names are now gone from the *entire* homepage. The argument doc's "bank grid = moat wall" idea (the one unfakeable thing competitors can't screenshot) has no home here anymore — candidate to live on `/caracteristicas` or `/integraciones` instead. Not decided.

**Next thread (in priority order, full list in README):** (1) wire rail cards → chapter anchors [mechanical]; (2) Dan security-claims Slack checklist [blocks Seguridad]; (3) verify stat numbers vs prod; (4) mobile pass; (5) Rails port. Page is still legitimately in terminal-move **(a) keep drafting** — `/tesote-plan` for the Rails port comes after Dan clears security.
